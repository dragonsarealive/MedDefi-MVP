'use client';

const Header = () => (
  <header className="w-full h-20 flex items-center justify-end px-8 bg-white border-b shadow-sm">
    <div className="text-gray-700 font-semibold">Dashboard Header (User info, calendar, etc. will go here)</div>
  </header>
);

export default Header; 